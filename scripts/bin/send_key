#!/bin/sh

echo "Press [CTRL+C] to stop.."

while :
do
    WINDOW=$(xdotool getactivewindow)
    NAME=$(xdotool getwindowclassname $WINDOW)
    echo $NAME
    if [[ $NAME == wowclassic.exe ]]
    then
        echo Sending keypress
        xdotool key alt+v
    fi
	sleep $1
done
